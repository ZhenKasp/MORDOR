import React from 'react';

const varification = props => (
  <div>
    <h4>Registration is almost complete</h4>
    <p>Check {props.email} to confirm your identity</p>
  </div>
);

export default varification;
